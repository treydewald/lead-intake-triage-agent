# Subagent Registry — Lead Intake Triage Agent

Full guidance: `docs/token-discipline.md` §7 ("Subagents"), `docs/plan-execute-review.md`
§Execute Phase. Most portfolio projects need zero project-specific subagents — the pipeline's own
16 steps, run as a single session following each prompt in turn, cover the standard build. Only
register an entry here if this project spawns a subagent for genuinely independent, well-scoped
work (e.g. a large batch of parallel-safe file edits across unrelated modules).

| Name | Responsibility | Isolation (`worktree`?) | When to use | When NOT to use |
|---|---|---|---|---|
| _(none defined yet)_ | | | | |
