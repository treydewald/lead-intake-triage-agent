# MCP Servers — Lead Intake Triage Agent

**Last Updated:** 2026-09-04 — None enabled. See `.claude/disabled-features.md` for why.

Which MCP servers this project's `.claude/settings.json` has enabled, and why. Most portfolio
projects need none — leave this as "None enabled" unless a step genuinely integrates with an
external system (e.g. a GitHub-heavy workflow, a Linear-tracked backlog).

| Server | Enabled? | Used for | Enabled by (step/date) |
|---|---|---|---|
| _(none yet)_ | | | |

Disabling a previously-enabled server? Move its row here to `.claude/disabled-features.md` with a
one-line reason instead of deleting the history.
